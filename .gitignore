.DS_Store
node_modules/
.astro/
**/__pycache__/

# All generated output: the multi-page site, the single-file bundle, and the PDFs.
build/output/

# Local environment files. Nothing in this repo needs secrets today, but Astro reads .env
# automatically, so keep them out of history by default.
.env
.env.*
!.env.example
